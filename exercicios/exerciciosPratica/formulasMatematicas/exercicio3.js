const nota1 = 10
const nota2 = 15
const nota3 = 22

// media aritmetica das notas
const media = (nota1 + nota2 + nota3) / 3;

alert("a media das notas é:" + media.toFixed(2));

console.log (media)

document.write("<h3>A média das notas é: " + media.toFixed(2) + "</h3>");